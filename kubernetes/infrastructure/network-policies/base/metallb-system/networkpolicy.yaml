---
# metallb-system: controller のみポリシー対象(speaker は hostNetwork のため NetworkPolicy 非対象)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: metallb-system
  labels:
    app.kubernetes.io/part-of: network-policies
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
# apiserver からの validating webhook 呼び出しを許可。
# 無いと IPAddressPool/L2Advertisement/LoadBalancer Service の変更が全て拒否される。
# 送信元は LAN node IP(192.168.0.0/24) または Flannel pod CIDR(10.42.0.0/16)になる。
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-webhook
  namespace: metallb-system
  labels:
    app.kubernetes.io/part-of: network-policies
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - ipBlock:
            cidr: 192.168.0.0/24
        - ipBlock:
            cidr: 10.42.0.0/16
      ports:
        - port: 9443
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-metrics-scrape
  namespace: metallb-system
  labels:
    app.kubernetes.io/part-of: network-policies
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: metrics
      ports:
        - port: 7472
          protocol: TCP
